#work

Ver padrões de commit -> https://github.com/iuricode/padroes-de-commits

git reset HEAD~1 -> Para voltar o ultimo commit

git reset --hard "id do commit" -> **voltar permanentemente** para o commit especificado e descartar todos os commits feitos depois dele

git checkout "id do commit" -> **examinar** o estado do código naquele commit, sem modificar o histórico de commits

git reset diretorio_do_aquivo

git branch -> para saber em qual branch eu estou